/*
*@param calls the number of bullets needed to win and makes a decision in this regard.
*
*
*/
public static void victoryPoints(){
	int victoryPointsLeft = 1000 - getTeamVictoryPoints();   //This calculates remaining points in the game.
	
	//is possible to win?
	int victoryPointsPossible = getTeamBullets()/10;    	 //Probably can be cleaned a little to save bytecode //do you know how many bytecodes this would use already?//No but this is an archon method so it won't be super significant as it has more bytecode capabilities than the others.
	if (victoryPointsLeft < victoryPointsPossible){
		donate(float getTeamBullets());		//This may not need the float thing
	}

}